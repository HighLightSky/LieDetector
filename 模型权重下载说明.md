# 预训练模型权重下载说明

## ✅ 已完成

根据测试结果，两个预训练模型都已成功加载：

### 1. FacesModel (MobileNetV3)
- **状态**: ✅ 已自动下载
- **来源**: timm 库自动从 PyTorch Hub 下载
- **位置**: `~/.cache/torch/hub/checkpoints/mobilenetv3_small_100_lamb-*.pth`
- **大小**: 约 10 MB
- **测试结果**: 通过 ✅

### 2. Wav2Vec2 (音频模型)
- **状态**: ✅ 已下载
- **来源**: Hugging Face transformers
- **模型**: `facebook/wav2vec2-base`
- **位置**: `~/.cache/huggingface/hub/models--facebook--wav2vec2-base/`
- **大小**: 约 360 MB
- **测试结果**: 通过 ✅

## 🔧 已修复的问题

### BatchNorm 单样本推理错误
**问题**: `Expected more than 1 value per channel when training`

**原因**: BatchNorm 层在训练模式下要求 batch_size > 1

**解决方案**: 
1. 修改了 `models/faces.py` 的 `forward` 方法，自动处理单样本情况
2. 修改了测试脚本，添加 `model.eval()` 和 `torch.no_grad()`

## 📝 使用建议

### 推理时务必使用 eval 模式
```python
model = FacesModel(device='cpu')
model.eval()  # 重要！

with torch.no_grad():
    output = model(input_tensor)
```

### 训练时使用 batch_size >= 2
```python
model.train()
# 确保 batch_size >= 2
batch_size = 4  # 或更大
```

## 🚀 下一步

现在模型权重已就绪，你可以：

1. **运行完整测试**: `python test_fusion_model.py`
2. **开始训练**: 需要准备数据集和训练脚本
3. **进行推理**: 需要实现视频预处理管道

## 📦 模型文件位置

### Windows 默认缓存位置
```
C:\Users\你的用户名\.cache\
├── torch\hub\checkpoints\          # MobileNetV3
└── huggingface\hub\                # Wav2Vec2
```

### 查看已下载的模型
```python
import os
from pathlib import Path

# PyTorch 模型
torch_cache = Path.home() / '.cache' / 'torch' / 'hub' / 'checkpoints'
print("PyTorch 模型:", list(torch_cache.glob('*.pth')))

# Hugging Face 模型
hf_cache = Path.home() / '.cache' / 'huggingface' / 'hub'
print("HF 模型:", list(hf_cache.glob('models--*')))
```

## ⚠️ 注意事项

1. **首次运行会下载模型**: 需要稳定的网络连接
2. **磁盘空间**: 确保至少有 1 GB 可用空间
3. **网络问题**: 如果下载失败，可以使用国内镜像:
   ```bash
   set HF_ENDPOINT=https://hf-mirror.com
   ```

## 🎉 测试通过

```
🔍 开始测试预训练模型...
==================================================
测试 FacesModel (MobileNetV3)...
✅ FacesModel 加载成功！
   输出形状: torch.Size([1, 2]) (应该是 [1, 2])
==================================================
测试 Wav2Vec2Encoder...
✅ Wav2Vec2Encoder 加载成功！
   模型设备: cpu
==================================================
🎉 所有模型加载成功！
==================================================
```
